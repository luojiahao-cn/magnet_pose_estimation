# 启动传感器校正服务
# 请求参数
string arm                          # 机械臂名称（如 "arm1"）
geometry_msgs/Pose start_pose       # 起始位姿（传感器阵列中心位置）
geometry_msgs/Point rotation_center # 旋转中心点（相对于start_pose）
string rotation_axis                # 旋转轴 ("x", "y", "z" 或组合)
float64[] angle_range               # 角度范围 [min_angle, max_angle] (弧度)
float64 angle_step                  # 角度步长 (弧度)
float64 velocity_scaling            # 运动速度缩放因子 (0.0-1.0)
float64 data_collection_duration    # 每个角度位置的数据采集持续时间 (秒)
bool save_raw_data                  # 是否保存原始数据到CSV
string output_directory             # 输出目录路径（CSV和校正参数）
---
# 响应
bool success
string message
string session_id                   # 校正会话ID（用于后续操作）

